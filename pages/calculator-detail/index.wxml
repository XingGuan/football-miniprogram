<!-- pages/calculator-detail/index.wxml -->
<view class="detail-page">
  <!-- 加载状态 -->
  <view class="loading-wrapper" wx:if="{{loading}}">
    <view class="loading-spinner"></view>
    <text class="loading-text">加载中...</text>
  </view>

  <!-- 错误状态 -->
  <view class="error-wrapper" wx:elif="{{error}}">
    <text class="error-text">{{error}}</text>
  </view>

  <!-- 详情内容 -->
  <scroll-view class="detail-content" scroll-y wx:else>
    <!-- 状态卡片 -->
    <view class="status-card {{record.status === 1 ? 'win' : record.status === 2 ? 'lose' : ''}}">
      <view class="status-row">
        <view class="status-left">
          <text class="status-icon">{{record.status === 1 ? '🎉' : record.status === 2 ? '😔' : '⏳'}}</text>
          <text class="status-text">{{record.statusDesc}}</text>
        </view>
        <view class="scheme-no">{{record.schemeNo}}</view>
      </view>
    </view>

    <!-- 投注信息 -->
    <view class="info-card">
      <view class="card-title-row">
        <text class="card-title">投注信息</text>
        <text class="time-value">{{record.createTimeStr}}</text>
      </view>
      <view class="info-row">
        <view class="info-left">
          <text class="info-value">{{record.totalBets}}注</text>
          <text class="info-value amount">{{record.multiple}}倍{{record.totalAmount}}元</text>
        </view>
        <view class="info-right">
          <!-- 中奖 -->
          <text class="info-value bonus win" wx:if="{{record.status === 1}}">中奖¥{{record.actualBonus}}</text>
          <!-- 未中奖 -->
          <text class="info-value bonus lose" wx:elif="{{record.status === 2}}">未中奖</text>
          <!-- 待开奖 -->
          <text class="info-value bonus" wx:elif="{{record.bonusRange}}">预计奖金¥{{record.bonusRange}}</text>
        </view>
      </view>
    </view>

    <!-- 选择的比赛 -->
    <view class="matches-card">
      <view class="card-title">比赛详情 ({{record.matchDetails.length}}场)</view>
      <view class="pass-row">
        <text class="pass-label">过关方式</text>
        <text class="pass-value">{{record.passTypesStr}}</text>
      </view>

      <view class="match-item" wx:for="{{record.matchDetails}}" wx:key="matchId">
        <view class="match-header">
          <text class="match-num">{{item.matchNumStr}}</text>
          <view class="match-teams">
            <text class="team-name home">{{item.homeTeamName}}</text>
            <text class="match-time">{{item.matchTime}}</text>
            <text class="team-name away">{{item.awayTeamName}}</text>
          </view>
        </view>

        <view class="match-options">
          <!-- 按玩法类型分组显示 -->
          <view class="option-group" wx:for="{{item.optionGroups}}" wx:for-item="group" wx:key="type">
            <view class="group-row">
              <!-- 玩法名称 -->
              <view class="group-title-box">
                <text class="group-title">{{group.typeDesc}}</text>
                <text class="group-goalline" wx:if="{{group.goalLine}}">({{group.goalLine}})</text>
              </view>
              <!-- 选项 -->
              <view class="group-options {{group.type === 'crs' || group.type === 'ttg' || group.type === 'hafu' ? 'vertical' : ''}}">
                <view class="option-cell {{opt.checked ? 'checked' : 'unchecked'}} {{opt.checked && opt.isHit ? 'hit' : ''}} {{opt.checked && opt.checkTime && !opt.isHit ? 'miss' : ''}} {{group.type === 'crs' || group.type === 'ttg' || group.type === 'hafu' ? 'vertical' : ''}}" wx:for="{{group.options}}" wx:for-item="opt" wx:key="value">
                  <text class="cell-value">{{opt.displayValue}}</text>
                  <text class="cell-odds">{{opt.odds}}</text>
                </view>
              </view>
              <!-- 开奖结果 -->
              <view class="group-result {{group.checkTime ? (group.isHit ? 'hit' : 'miss') : 'pending'}}">
                <text class="result-value">{{group.matchResultDesc || '待开奖'}}</text>
              </view>
            </view>
          </view>
        </view>
      </view>
    </view>

    <!-- 底部占位 -->
    <view class="bottom-placeholder"></view>
  </scroll-view>

  <!-- 底部操作栏 -->
  <view class="bottom-share-bar" wx:if="{{record}}">
    <!-- 推荐按钮（仅在非大厅来源时显示） -->
    <button class="share-btn" wx:if="{{!isFromHall}}" bindtap="onRecommend" loading="{{recommending}}">
      <text class="share-icon">👍</text>
      <text class="share-text">推荐</text>
    </button>
    <!-- 导出图片按钮（暂时隐藏） -->
    <!-- <button class="share-btn" bindtap="onExportImage" loading="{{exporting}}">
      <text class="share-icon">📷</text>
      <text class="share-text">保存图片</text>
    </button> -->
    <!-- 分享按钮 -->
    <button class="share-btn" wx:if="{{isFromHall}}" open-type="share">
      <text class="share-icon">📤</text>
      <text class="share-text">分享</text>
    </button>
  </view>

  <!-- 隐藏的 canvas 用于生成图片 -->
  <!-- <canvas type="2d" id="shareCanvas" style="position: fixed; left: -9999px; top: -9999px;"></canvas> -->
</view>
