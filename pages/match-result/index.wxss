/* pages/match-result/index.wxss - 赛果列表 */
.result-page {
  height: 100vh;
  background: linear-gradient(180deg, #f8f9fb 0%, #f5f7fa 100%);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.loading-wrapper, .error-wrapper {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 200rpx 40rpx;
}

.loading-spinner {
  width: 60rpx;
  height: 60rpx;
  border: 4rpx solid #f0f0f0;
  border-top-color: #667eea;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
}

@keyframes spin { to { transform: rotate(360deg); } }

.loading-text, .error-text {
  margin-top: 20rpx;
  font-size: 26rpx;
  color: #666;
}

.retry-btn {
  margin-top: 24rpx;
  padding: 16rpx 48rpx;
  background: #667eea;
  color: #fff;
  border-radius: 32rpx;
  font-size: 26rpx;
}

.main-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

/* 页面头部 */
.page-header {
  display: flex;
  flex-direction: column;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  padding: 32rpx 24rpx;
  color: #fff;
  box-shadow: 0 4rpx 16rpx rgba(102, 126, 234, 0.2);
}

.page-title {
  font-size: 36rpx;
  color: #fff;
  font-weight: 700;
  margin-bottom: 8rpx;
  letter-spacing: 0.5rpx;
}

.page-desc {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.8);
  font-weight: 400;
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  flex: 1;
  padding: 100rpx 40rpx;
  color: #999;
}

.empty-icon {
  font-size: 120rpx;
  margin-bottom: 24rpx;
  opacity: 0.6;
}

.empty-text {
  font-size: 32rpx;
  color: #666;
  font-weight: 600;
  margin-bottom: 12rpx;
}

.empty-tip {
  font-size: 26rpx;
  color: #999;
  text-align: center;
}

.results-scroll {
  flex: 1;
  height: 0;
  padding: 16rpx 12rpx 24rpx 12rpx;
}

/* 赛果卡片 */
.result-item {
  background: #fff;
  border-radius: 16rpx;
  margin-bottom: 16rpx;
  overflow: hidden;
  box-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.08);
  transition: all 0.3s ease;
  position: relative;
}

.result-item:active {
  transform: translateY(2rpx);
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.12);
}

/* 状态指示条 */
.result-status-bar {
  height: 4rpx;
  width: 100%;
  background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
}

.result-item.finished .result-status-bar {
  background: linear-gradient(90deg, #52c41a 0%, #85ce61 100%);
}

.result-item.ongoing .result-status-bar {
  background: linear-gradient(90deg, #ff9800 0%, #ffb84d 100%);
}

.result-item.upcoming .result-status-bar {
  background: linear-gradient(90deg, #d9d9d9 0%, #e8e8e8 100%);
}

/* 头部信息 */
.result-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 12rpx 20rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.league-info {
  display: flex;
  align-items: center;
  gap: 12rpx;
  flex: 1;
}

.league-name {
  font-size: 22rpx;
  color: #ff9800;
  font-weight: 600;
}

.status-badge {
  font-size: 16rpx;
  padding: 3rpx 8rpx;
  border-radius: 4rpx;
  background: #f0f0f0;
  color: #999;
  font-weight: 500;
}

.status-badge.finished {
  background: rgba(82, 196, 26, 0.15);
  color: #52c41a;
}

.status-badge.ongoing {
  background: rgba(255, 152, 0, 0.15);
  color: #ff9800;
}

.status-badge.upcoming {
  background: rgba(200, 200, 200, 0.2);
  color: #999;
}

.time-info {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 18rpx;
  color: #ccc;
}

.date {
  font-weight: 500;
}

.time {
  font-weight: 400;
}

/* 对阵和比分区 */
.match-section {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20rpx;
  gap: 12rpx;
}

.vs-info {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex: 1;
  gap: 12rpx;
}

.team-name {
  font-size: 24rpx;
  color: #333;
  font-weight: 600;
  flex: 1;
  text-align: center;
  line-height: 1.3;
  word-break: break-all;
}

.team-name.home {
  text-align: right;
}

.team-name.away {
  text-align: left;
}

.score-box {
  display: flex;
  align-items: center;
  justify-content: center;
  min-width: 120rpx;
  background: linear-gradient(135deg, #fff0f0 0%, #fff5f5 100%);
  border: 2rpx solid #ffe8e8;
  border-radius: 12rpx;
  padding: 16rpx 20rpx;
}

.score {
  font-size: 48rpx;
  font-weight: 900;
  font-family: 'Arial', 'Courier New', monospace;
  letter-spacing: 3rpx;
  color: #ff3b30;
  line-height: 1;
}

/* 展开按钮 */
.expand-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 44rpx;
  height: 44rpx;
  cursor: pointer;
}

.expand-icon {
  font-size: 20rpx;
  color: #999;
  transition: transform 0.3s ease;
  font-weight: 600;
}

.expand-icon.expanded {
  transform: rotate(180deg);
  color: #667eea;
}

/* 结果行 */
.result-row {
  display: flex;
  justify-content: center;
  padding: 10rpx 20rpx;
  border-bottom: 1rpx solid #f0f0f0;
}

.result-badge {
  font-size: 18rpx;
  padding: 8rpx 16rpx;
  border-radius: 8rpx;
  background: #f0f0f0;
  color: #999;
  font-weight: 600;
}

.result-badge.H {
  background: rgba(255, 107, 107, 0.15);
  color: #ff6b6b;
}

.result-badge.D {
  background: rgba(102, 126, 234, 0.15);
  color: #667eea;
}

.result-badge.A {
  background: rgba(82, 196, 26, 0.15);
  color: #52c41a;
}

/* 展开的开奖结果区 */
.odds-compact {
  padding: 12rpx 20rpx;
  display: flex;
  flex-wrap: wrap;
  gap: 8rpx;
  background: #f8f9fb;
  border-top: 1rpx solid #f0f0f0;
}

.odds-item {
  display: flex;
  align-items: center;
  gap: 6rpx;
  font-size: 16rpx;
  padding: 8rpx 12rpx;
  background: #fff;
  border-radius: 6rpx;
  border: 1rpx solid #e8e8e8;
  flex: 0 1 calc(50% - 4rpx);
}

.odds-label {
  color: #999;
  font-weight: 600;
  min-width: 40rpx;
  font-size: 14rpx;
}

.odds-desc {
  color: #333;
  font-weight: 500;
  font-size: 14rpx;
  text-overflow: ellipsis;
  overflow: hidden;
  white-space: nowrap;
}

.bottom-placeholder {
  height: 20rpx;
}
